#pragma once

/**
 * Replace the PEM blocks below with your actual certificates/keys.
 * Keep the newline characters (\n) intact.
 */
extern const char mqtt_server_root_cert_pem[];
extern const char mqtt_client_cert_pem[];
extern const char mqtt_client_key_pem[];

extern const char ota_server_root_cert_pem[];
